apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: gpt-oss-pvc
  namespace: default
  labels:
    app: my-app
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
  volumeMode: Filesystem
